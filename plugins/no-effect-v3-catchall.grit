engine biome(1.0)
language js

`Effect.catchAll($args)` where {
  register_diagnostic(message="Effect v4: catchAll was removed. Use Effect.catchCause to handle all causes, or Effect.catch for a specific error tag.", severity="error")
}
