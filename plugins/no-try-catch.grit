engine biome(1.0)
language js

`Effect.gen($fn)` as $outer where {
  $fn <: contains JsCatchClause() as $catch where {
    register_diagnostic(span=$catch, message="Avoid try/catch inside Effect.gen. Use Effect.try, Effect.tryPromise, or typed error channels to keep errors in the Effect pipeline.", severity="warning")
  }
}
