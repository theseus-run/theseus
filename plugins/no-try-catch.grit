engine biome(1.0)
language js

JsCatchClause() as $catch where {
  register_diagnostic(span=$catch, message="Avoid try/catch. Use Effect.try, Effect.tryPromise, or typed error channels to keep errors in the Effect pipeline.", severity="warning")
}
