language js(typescript)

`$expr.pipe($a).pipe($b).pipe($c)` where {
  register_diagnostic(message="Avoid chained .pipe().pipe() ladders. Combine all operators in a single .pipe() call for a flat, readable pipeline.", severity="warning")
}
