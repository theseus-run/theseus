engine biome(1.0)
language js

`Effect.Do` as $m where {
  register_diagnostic(span=$m, message="Avoid Effect.Do. Use a single flat pipe-based Effect flow or Effect.gen with direct yields.", severity="warning")
}
