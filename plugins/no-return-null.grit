engine biome(1.0)
language js

`return null` where {
  register_diagnostic(message="Avoid returning null. Use Option.none() for absence, Effect.fail for errors, or a tagged union.", severity="warning")
}
