language js(typescript)

`return null` where {
  register_diagnostic(message="Avoid returning null. Use Option.none() for absence, Effect.fail for errors, or a tagged union for explicit 'nothing happened' semantics.", severity="warning")
}
