engine biome(1.0)
language js

`"@effect/schema"` where {
  register_diagnostic(message="Effect v4: @effect/schema is gone. Import from 'effect' instead: import { Schema } from 'effect'.", severity="error")
}
