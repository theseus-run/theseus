engine biome(1.0)
language js

`Cause.isInterruptedOnly($args)` as $m where {
  register_diagnostic(span=$m, message="Effect v4: Cause.isInterruptedOnly was renamed to Cause.hasInterruptsOnly. Load skill: effect-errors-schema.", severity="error")
}
