engine biome(1.0)
language js

`Cause.isInterruptedOnly($args)` where {
  register_diagnostic(message="Effect v4: Cause.isInterruptedOnly was renamed to Cause.hasInterruptsOnly.", severity="error")
}
