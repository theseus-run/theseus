engine biome(1.0)
language js

`Effect.$outer(Effect.$inner($args))` where {
  register_diagnostic(message="Avoid nested Effect combinators (Effect ladder). Build context once, then use a single flat pipeline.", severity="warning")
}
