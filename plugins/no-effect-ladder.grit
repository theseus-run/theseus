language js(typescript)

`Effect.$outer(Effect.$inner($args))` where {
  register_diagnostic(message="Avoid nested Effect combinators (Effect ladder). Build context once with Effect.all/Effect.map, then use a single flat pipeline.", severity="warning")
}
