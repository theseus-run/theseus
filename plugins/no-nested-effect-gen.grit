engine biome(1.0)
language js

`Effect.gen($fn)` as $outer where {
  $fn <: contains `yield* Effect.gen($inner)` as $innerCall,
  register_diagnostic(span=$innerCall, message="Avoid directly yielding nested Effect.gen. Flatten the generator or extract a named Effect-returning helper.", severity="warning")
}
