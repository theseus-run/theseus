engine biome(1.0)
language js

or {
  `Layer.succeed($tag, $impl)`,
  `Layer.effect($tag, $impl)`,
  `Layer.scoped($tag, $impl)`
} as $m where {
  register_diagnostic(span=$m, message="Effect v4 (beta.50): Layer constructors have both curried and two-arg forms. Curried Layer.succeed(Tag)(impl) is preferred for consistency with partial application patterns.", severity="warning")
}
