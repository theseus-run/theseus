engine biome(1.0)
language js

`Effect.gen(function*($adapter) { $body })` as $m where {
  $adapter <: r"^\w+$",
  register_diagnostic(span=$m, message="Effect v4 (beta.50): remove the adapter parameter. Use yield* effect directly instead of yield* adapter(effect).", severity="error")
}
