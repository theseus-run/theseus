language js(typescript)

`Effect.gen(function*($adapter) { $body })` where {
  $adapter <: r"^\w+$",
  register_diagnostic(span=$adapter, message="Effect v4: remove the adapter parameter. Use `yield* effect` directly instead of `yield* adapter(effect)`.", severity="error")
}
