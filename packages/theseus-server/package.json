{
  "name": "@theseus.run/server",
  "version": "0.0.1",
  "description": "Theseus server — Effect RPC + HttpServer + SQLite persistence",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@effect/platform-bun": "catalog:effect",
    "@theseus.run/core": "workspace:*",
    "@theseus.run/runtime": "workspace:*",
    "@theseus.run/tools": "workspace:*",
    "effect": "catalog:effect",
    "zod": "4.3.6"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "typescript": "^5.9.3"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
