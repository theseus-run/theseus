/**
 * EventStore — STUB.
 *
 * The old EventStore (useSyncExternalStore bridge for UIEvent/NodeStatus)
 * has been removed. The new architecture uses Dispatch.Handle.events directly.
 *
 * This file will be rewritten when icarus-cli is wired to the new primitives.
 * The store will consume DispatchEvent instead of the old UIEvent type.
 */
